{
  "name": "my-package",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "build": "babel src -d lib",
    "serve": "json-server --watch db.json --port 5000 --routes routes.json"
  }
}
